Never mark a @agents/plans/[PLAN_NAME].md item complete until its acceptance criteria have been verified.

At the end of every session:
1. update @agents/plans/[PLAN_NAME].md,
2. update @agents/STATE.md,
3. update @agents/CHANGELOG.md,
4. run relevant tests,
5. commit the work.